Config = {}


-- Notification position on screen.
--   'top-left'      'top-center'      'top-right'
--   'center-left'   'center'          'center-right'
--   'bottom-left'   'bottom-center'   'bottom-right'

Config.Position = 'top-center'

-- How long (ms) the announcement stays on screen
Config.AnnouncementDuration = 10000

-- Notification sound volume (0.0 - 1.0)
Config.SoundVolume = 0.5

-- Play a sound when a notification appears
Config.EnableSound = true

-- Check GitHub for resource updates on server start
Config.CheckForUpdates = true
